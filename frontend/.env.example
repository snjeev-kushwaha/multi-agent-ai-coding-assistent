# Vite proxies /api to the backend in local dev (see vite.config.ts), so no
# API base URL env var is needed there. In production, the nginx config
# (nginx.conf) handles the same proxying -- point BACKEND_HOST at your API
# service/domain when deploying.
